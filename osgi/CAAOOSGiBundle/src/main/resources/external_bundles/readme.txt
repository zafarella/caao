The directory contains the external osgi bundles needed for the caao bundle:
-- com.mysql.jdbc_1.0.0.jar  -the mySQL driver converted into the OSGi style bundle.
	the exported packages include:
	.................
	a lot packages for mysql connectivity.
	
Before installing the bundle into the framework, make sure that axis2-osgi bundle is installed
or the imported packages of this bundle provided by some other bundle in the system.